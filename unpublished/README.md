# Unpublished

This folder contains a restore script to bring the About Me pages and references back.

Usage:

```bash
./unpublished/restore-about-me.sh
```

Notes:
- The script restores files from the current git `HEAD`.
- Any local edits to those files will be overwritten when you run the script.
